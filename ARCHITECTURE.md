# Auralis Mobile 架构路线

本文档记录 Auralis Mobile 当前暂定的技术路线和架构约束。它描述目标设计，不代表所有能力已经在当前代码中实现；后续实现应以本文件、`PRODUCT.md` 和 `METADATA.md` 的边界为依据。

## 1. 技术栈

- 使用 Kotlin 作为主要开发语言。
- 使用 Jetpack Compose 构建 UI。
- 使用 AndroidX Media3 / ExoPlayer 负责音频播放。
- 使用 Coil 负责图片展示，主要服务于专辑封面等本地图片内容。
- 使用 Kotlin Coroutines / Flow 处理异步任务、状态流和扫描过程中的数据更新。
- 使用 Room 作为 Auralis 自己的本地数据库。
- 使用 DataStore 保存轻量设置。
- v1 暂不引入 Hilt 或 Koin；只有在项目规模和依赖管理复杂度确实需要时，才重新评估引入依赖注入框架。

核心 Android 逻辑必须使用标准 Android / AndroidX API。ColorOS 的特殊行为只能作为后续独立的 OEM 适配，不能成为核心架构前提。

当前只考虑手机内部存储，不扩展到平板、TV、车机、Wear OS、IoT 或其他外部设备形态。

## 2. 播放、后台与系统媒体控制

后台播放和系统媒体控制采用 AndroidX Media3 的标准方案：

- 使用 `MediaLibraryService` 承载后台播放服务和媒体库访问能力。
- 使用 `MediaSession` 连接播放器、系统媒体控件、通知栏及其他标准媒体控制入口。
- 使用 Media3 / ExoPlayer 承担播放队列、播放状态和音频输出的基础能力。

实现应保持前台 UI 与后台播放服务之间的状态边界清晰，避免把播放生命周期绑定到单个 Compose 页面。

## 3. 曲库目录与权限

### 目录选择

使用 Storage Access Framework 的 `ACTION_OPEN_DOCUMENT_TREE` 让用户选择一个或多个曲库目录。

- 每个用户选择的目录都作为一个曲库根目录。
- 目录授权需要持久化，以便后续重新打开应用和手动扫描时继续访问。
- 当前只考虑手机内部存储。
- 不以 MediaStore 作为 Auralis 曲库的真源。

### 扫描来源

Auralis 直接扫描用户授权的目录树，并读取音频文件及其元数据。曲库内容的发现、解析、身份判断和可用性状态均以用户授权目录和 Auralis 自己的数据库为基础，而不是以系统媒体库的收录结果为基础。

曲库目录采用递归扫描。单个文件的解析或播放失败不能中断整个扫描任务，扫描应继续处理其他目录和文件，并为失败文件保留诊断信息。

## 4. 本地数据分层

### Room：曲库与用户数据

Room 保存 Auralis 自己管理的本地数据，包括曲库条目、目录根、解析出的元数据、文件可用性状态、错误原因、歌单及其手动排序等。

Room 中的曲库数据必须能够表达以下状态：

- 文件正常解析并可播放。
- 文件存在但无法解析或播放。
- 文件所在曲库目录暂时不可访问。
- 文件仍保留为曲库条目，但当前缺少可用的文件内容。

### DataStore：轻量设置

DataStore 只保存轻量设置，例如界面偏好、播放相关选项和其他不适合进入曲库关系数据的用户配置。需要结构化查询、排序、关联或恢复的用户数据应进入 Room，而不是塞入 DataStore。

## 5. 曲库条目身份与去重

### 根目录重叠

多个曲库根目录的内容可能重叠。同一个实际文件不能因为被多个根目录覆盖而重复入库，扫描和入库流程必须提供稳定的去重判断。

### 文件移动与身份连续性

- 文件仅移动路径时，仍应视为同一个曲库条目，不能仅因路径变化就创建新条目。
- 文件格式变化或重新压制时，如果核心身份元数据未变化，可以继续关联原条目。
- 如果核心身份元数据被大幅修改，允许将其识别为新条目。
- 不同 Album 或不同发行版本必须是不同的曲库条目。

条目身份判断应优先使用稳定的文件和元数据证据，不得把当前路径作为唯一身份。具体字段规则以 `METADATA.md` 定义的 Album、Album Artist、Date、Disc Number 和 Track Number 约束为准。

### 重复候选选择

当扫描结果产生多个代表同一曲目或同一身份的候选文件时，按以下顺序选择优先候选：

1. 优先选择能够正常解析且能够正常播放的文件。
2. 若仍有多个候选，按曲库根目录的添加顺序决定优先级。
3. 若仍在同一根目录内，则按稳定的相对路径排序选择。

选择规则必须稳定、可重复，并且不能依赖不可预测的扫描顺序。

## 6. 扫描异常与不可访问目录

### 暂时不可访问的目录

曲库目录暂时不可访问时，不应立即删除该目录中的歌曲。系统应保留相关曲库条目，并将目录标记为不可访问，等待用户恢复权限、目录可用性或再次扫描后再更新状态。

### 无法解析或播放的文件

无法解析或播放的文件仍保留在曲库中，不静默丢弃：

- 在 UI 中以灰显状态表示不可用。
- 提供可理解的错误原因。
- 不得让单个坏文件中断整个扫描任务。

错误状态应尽量区分访问失败、格式不支持、元数据解析失败、播放器无法打开等原因，以便用户诊断和后续恢复。

## 7. 诊断与可恢复性

诊断系统需要支持将扫描、解析、播放和目录访问相关的诊断信息导出为文本或 JSON 日志。

导出内容应帮助定位具体目录、文件、阶段和错误原因，同时避免把诊断导出设计成修改音频文件或依赖网络服务的流程。

## 8. 备份边界

备份只保存不可重建或重建成本较高的用户数据，例如：

- 自建歌单及其歌曲顺序
- DataStore 中的用户设置
- 其他必要的用户数据

音乐文件和可由授权目录重新扫描得到的曲库本身不作为必须备份的对象。音乐库可以通过重新授权目录并重新扫描重建。

## 9. 元数据解析层（Phase 2A 调研结论）

本节记录 Technical Spike Phase 2A 已确认的结论，调研细节见 `docs/METADATA_ENGINE_RESEARCH.md`。方案已确定，但原生构建链路尚未实机验证，验证属于 Phase 2B。

- 元数据读取复用 Auxio/Musikr 的“文件/URI → 原始 TagLib 元数据”层（TagLib JNI），不从零实现 ID3、MP4 Atom、Vorbis Comment、FLAC Picture 解析。
- 该层只产出原始结果：按容器分开的 `id3v2` / `xiph` / `mp4` 三组 `key → List<String>`、音频属性（mimeType、时长、比特率、采样率）以及一张内嵌封面。它不做任何字段语义解释。
- Auralis 的元数据解释规则完全由 Auralis 自己实现并位于该层之上：Artist 与 Album Artist 严格区分、Artist 缺失时不退化为 Composer、不自动补 Various Artists、多值字段只用英文分号 `;` 分隔、缺失字段保持未知、Date 按 Auralis 规则处理。`METADATA.md` 是唯一依据。
- 该层通过 `ContentResolver.openFileDescriptor(uri, "r")` 读取 SAF 内容，不使用真实绝对文件路径，也不依赖 MediaStore。
- 原生部分使用 CMake + Android NDK 构建，TagLib 以源码子模块参与同一次 CMake 构建，不依赖 `sh`、WSL 或其他 Unix-only 脚本。
- v1 的 native ABI 只编译 `arm64-v8a`。
- 该层不引入 Room、播放链路、缓存或 UI 依赖。

## 10. 曲库持久化与对账架构（Phase 3B 规范）

### 10.1 核心分层（Logical Track vs Physical TrackSource）
Auralis 严格解耦物理连续性与逻辑呈现：
- **`TrackSourceEntity`（物理源）**：通过 `(provider, documentId)` 作为绝对物理连续性主键。文件重命名或跨目录移动时保持物理源连续；保存格式属性、文件修改时间（`modifiedMs`）、文件大小（`sizeBytes`）及可用性状态（`AVAILABLE` / `MISSING` / `UNREACHABLE`）。
- **`TrackEntity`（逻辑曲目）**：业务和播放队列的主体，拥有独立的永久 `TrackId`（UUID）。
- **`SourceMetadataEntity`（抽取缓存）**：为每个物理源保存最近一次成功解析的结构化元数据快照。
- **`TrackActiveSourceEntity`（选优指针）**：指示当前逻辑曲目的胜出物理源。

### 10.2 权威数据库与事务边界
- Room 作为 Auralis 曲库的权威真源。
- 扫描发现与 TagLib 抽取完全位于数据库事务外部；数据库持久化采用分批原子事务（100~200 条批量写入），避免长锁与高频 IO 瓶颈。
- 只有成功扫描的 scope 内部失联源才标记为 `MISSING`；根目录整体故障或单目录不可达（`DirectoryUnavailable`）时保持 `UNREACHABLE`，绝对不误删曲目。

### 10.3 抽取缓存体系（Hit / Stale / Miss）
- 扫描器比对文件的 `(modifiedMs, sizeBytes)` 与数据库记录。
- **HIT**：源未变更且曾成功解析，直接从 `SourceMetadataEntity` 水合内存对象，**零 JNI 调用、零底层文件 IO**。
- **STALE / MISS**：新源或已修改文件触发 TagLib 重新抽取并更新缓存快照。

### 10.4 墓碑与恢复机制（Tombstone & Reconnect）
- 当某个逻辑曲目的所有物理源都确认缺失时，`TrackEntity` 不物理删除，作为 Tombstone 驻留。
- 当具备相同 Strong TrackKey 的物理源重新出现时，自动重连恢复该 `TrackId`，确保播放历史与歌单引用不丢失。

